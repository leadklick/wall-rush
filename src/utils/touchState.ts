// ─── Shared mutable touch-button state ───────────────────────────────────────
// Written by TouchControls, read by useInput via getter proxy

export const touchState = {
  left:  false,
  right: false,
  jump:  false,
  slide: false,
  boost: false,
}
